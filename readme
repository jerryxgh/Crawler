简单爬虫说明：
Crawler: 爬虫类，流程是根据初始化的链接种子获得更多的链接，并提取出页面中的代码保存到磁盘。
HtmlParser: 使用 jsoup 分析出页面的代码等信息。
LinkDB: 链接数据库，完成链接的存储并维护一个队列用于 Crawler 抓取网页用。
LinkFilter: 链接过滤器，这是一个接口，用来过滤链接，使只有符合条件的链接才能被用于进一步抓取。
Queue: 实现了一个队列。